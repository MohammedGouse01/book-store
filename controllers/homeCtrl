const home= (req,res)=>{
    res.status(200);
    res.send('Welcome to the Book Store');
};

const health= (req,res)=>{
    const info={
        status:'up'
    };
    res.status(200);
    res.json(info);
}

module.exports={
    home,
    health,
};